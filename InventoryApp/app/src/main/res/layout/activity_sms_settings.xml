<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <!-- Screen Header wrapped in AppBarLayout for proper spacing -->
    <com.google.android.material.appbar.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:fitsSystemWindows="true"
        android:background="?attr/colorPrimary"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            app:title="SMS Notification Settings"
            app:titleTextColor="?attr/colorOnPrimary"
            app:navigationIcon="@drawable/sms"/>
    </com.google.android.material.appbar.AppBarLayout>

    <!-- Main Container -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/sms_setting_row"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingHorizontal="24dp"
        android:paddingVertical="16dp"
        android:background="?attr/colorTertiaryContainer"
        app:layout_constraintTop_toBottomOf="@id/app_bar_layout"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Title -->
        <TextView
            android:id="@+id/sms_toggle_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Low Inventory SMS Alerts"
            android:textAppearance="?attr/textAppearanceTitleMedium"
            android:textColor="?attr/colorOnSurface"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/sms_toggle_switch"
            android:layout_marginEnd="16dp"/>

        <!-- The main SMS toggle switch -->
        <com.google.android.material.switchmaterial.SwitchMaterial
            android:id="@+id/sms_toggle_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="false"
            android:scaleX="1.2"
            android:scaleY="1.2"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:thumbTint="?attr/colorPrimary"
            android:trackTint="?attr/colorSecondaryVariant"/>

        <!-- Description Text -->
        <TextView
            android:id="@+id/sms_toggle_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Receive automated alerts when stock levels drop below the threshold (e.g., low inventory, upcoming event)."
            android:textAppearance="?attr/textAppearanceBodyMedium"
            app:layout_constraintTop_toBottomOf="@id/sms_toggle_title"
            app:layout_constraintStart_toStartOf="@id/sms_toggle_title"
            app:layout_constraintEnd_toEndOf="@id/sms_toggle_title"
            android:textColor="?attr/colorOnTertiaryContainer"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Permission Status / Prompt Area -->
    <TextView
        android:id="@+id/permission_status_text"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="24dp"
        android:layout_marginTop="16dp"
        android:background="@android:color/holo_red_light"
        android:padding="12dp"
        android:text="⚠️ SMS Permission Denied: To enable automated SMS alerts, please tap the switch and grant the requested 'Send SMS' permission."
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="@android:color/white"
        android:visibility="gone"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/sms_setting_row"
        tools:visibility="visible" />

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:itemIconTint="@color/menu_color_selector"
        app:itemTextColor="@color/menu_color_selector"
        app:labelVisibilityMode="labeled"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:menu="@menu/bottom_nav_menu" />

</androidx.constraintlayout.widget.ConstraintLayout>
